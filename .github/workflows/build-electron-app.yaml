name: Build Firefly Electron App

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version number to release (e.g., 3.3.3)"
        required: true
  # NOTE: only the release event, deliberately. Publishing a release also creates
  #  (or pushes) the tag, so also listening for `push: tags: v*` fired this whole
  #  matrix a second time for the same release.
  release:
    types: [published]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set version variable
        id: vars
        shell: bash
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION="${{ github.event.release.tag_name }}"
          fi
          VERSION=${VERSION#v}  # strip leading v if present
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # gate on VERSION rather than the ref: a workflow_dispatch run happens on a
      #  branch, so a refs/tags/ check would skip this and ship the stale version
      - name: Update package.json version
        if: env.VERSION != ''
        run: |
          node -e "
            const fs = require('fs');
            const path = 'electron-app/package.json';
            const pkg = JSON.parse(fs.readFileSync(path, 'utf8'));
            let version = process.env.VERSION;
            if (version.startsWith('v')) version = version.slice(1);
            pkg.version = version;
            fs.writeFileSync(path, JSON.stringify(pkg, null, 2));
          "
        env:
          VERSION: ${{ env.VERSION }}        

      - name: Install dependencies
        run: npm install
        working-directory: electron-app

      - name: Build with electron-builder
        run: npx electron-builder --${{ matrix.os == 'ubuntu-latest' && 'linux' || (matrix.os == 'windows-latest' && 'win' || 'mac') }} --x64
        working-directory: electron-app

      - name: List dist contents
        run: ls dist
        working-directory: electron-app

      - name: Make build directory
        run: mkdir build
        working-directory: electron-app

      - name: Rename and move artifacts
        shell: bash
        run: |
          APP="firefly-electron-v${{ env.VERSION }}"

          if [[ "${{ matrix.os }}" == "windows-latest" ]]; then
            if compgen -G "dist/*.exe" > /dev/null; then
              mv dist/*.exe build/${APP}-win-x64-installer.exe
            fi
          elif [[ "${{ matrix.os }}" == "ubuntu-latest" ]]; then
            if compgen -G "dist/*.AppImage" > /dev/null; then
              mv dist/*.AppImage build/${APP}-linux-x64-installer.AppImage
            fi
            if compgen -G "dist/*.deb" > /dev/null; then
              mv dist/*.deb build/${APP}-linux-amd64.deb
            fi
          elif [[ "${{ matrix.os }}" == "macos-latest" ]]; then
            if compgen -G "dist/*.dmg" > /dev/null; then
              mv dist/*.dmg build/${APP}-mac-x64.dmg
            fi
            if compgen -G "dist/*.pkg" > /dev/null; then
              mv dist/*.pkg build/${APP}-mac-x64.pkg
            fi
          fi
        working-directory: electron-app

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-build
          path: electron-app/build/*
          retention-days: 7


      - name: Create Release
        if: github.event_name == 'release'
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          files: electron-app/build/**  # all files in build folder
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}