#!/bin/bash
# Preinstall script for macOS pkg
# the pkg installer expects a file named preinstall (or postinstall)
set -e
INSTALL_DIR="$1"

echo "Running macOS prepare.sh preinstall script..."
bash "$INSTALL_DIR/resources/scripts/prepare.sh" "$INSTALL_DIR"
